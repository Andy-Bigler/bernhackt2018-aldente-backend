# Backend

## Scripts
`npm run build`  
`npm run dev`  
`npm run start`  
`npm run prod`

## Docker

Build docker image:

```bash
docker build -t aldente-backend .
```

Run docker image:

```bash
docker run -p 3000:3000 aldente-backend
```